import LoadingModal from "../components/LoadingModal"

// we're not going to trigger this loading ourselves.
// this is only going to be triggered by next13
const Loading = () => {
  return <LoadingModal />
}

export default Loading;